apiVersion: v1
kind: Secret
metadata:
  name: tailscale-state
  namespace: tailscale-subnet-router
# This secret is intentionally empty. The tailscale pod writes its state here at runtime.
# Flux uses Server-Side Apply and does not own the `data` field (it is absent from this
# manifest), so reconciliation will never overwrite the pod-written state.
