apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The CRDs are cluster-scoped, so there is nothing to re-namespace here. This
# overlay is kept so consumers referencing the multitenancy path keep working.
resources:
  - ../base
