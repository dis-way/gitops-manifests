apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: platform-altinn-no
  namespace: dis-tls-cert
spec:
  secretName: platform-altinn-no
  issuerRef:
    name: digicert-dis-tls-cert
    kind: ClusterIssuer
  dnsNames:
    - platform.altinn.no
